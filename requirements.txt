mcp>=2.0.0

# ── Phase 2 (Windows accessibility API 統合) 用 extras ───────────
# pywinauto は Windows 専用。 uia backend で ValuePattern / InvokePattern /
# UI ツリー探索 を提供。 Pillow は capture_as_image が要求。
# 現時点 (v0.2.0-alpha) では find_element のみ実装。 click / type SetValue /
# screenshot / excel_aggregate は chat-Claude 分担で 追加予定。
#
# 手動 install (Windows のみ):
#   pip install "pywinauto>=0.6.9" "Pillow>=10.0"
#
# 非 Windows / pywinauto 未 install の環境では find_element は
# graceful error を返し、 他 11 action kinds は影響を受けない。
